import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  appWrapper: {
    display: "flex",
    flexDirection: "column",
    minHeight: "100vh",
  },
  mainContentWrapper: {
    flex: "1",
  },
});
